program fib (n)
is
  a[0] := 1;
  a[1] := 1;
  idx := 2;
  while idx < n do
    a[idx] := a[idx - 1] + a[idx - 2];
    idx := idx + 1;
  end
  assert forall i, ((i >= 2 && i < n) ==> a[i] = a[i - 1] - a[i - 2]);
end