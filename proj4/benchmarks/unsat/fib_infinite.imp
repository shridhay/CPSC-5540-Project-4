program fib_infinite ()
is
  a[0] := 1;
  a[1] := 1;
  idx := 2;
  while 1 = 1 do
    a[idx] := a[idx - 1] + a[idx - 2];
    idx := idx + 1;
    assert forall i, ((i >= 2 && i < idx) ==> a[i] = a[i - 1] - a[i - 2]);
  end
end