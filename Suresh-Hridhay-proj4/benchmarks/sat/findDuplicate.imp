program findDuplicate(n x[])
pre n > 0
pre exists i j, (0 <= i && i < n && 0 <= j && j < n && x[i] = x[j])
is
  dup1 := 0;
  dup2 := 0;
  i := 0;
  while i < n do
    j := i+1;
    while j < n do
      if x[i] = x[j] then
        dup1 := i;
        dup2 := j;
      end
      j := j + 1;
    end
    i := i + 1;
  end
  assert x[dup1] = x[dup2];
end
